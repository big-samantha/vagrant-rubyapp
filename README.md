# Minimal Ruby Vagrant ENV
1. Copy `rubyversion.example` to `rubyversion`. 
1. Set version of Ruby you'd like to work with. 
1. `vagrant up`

Requires Vagrant and a functional Vagrant backend (VirtualBox, Vagrant Fusion, etc.)
